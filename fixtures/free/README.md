# Disposable free-authority exports

These public packages come from the AgentBorn application's disposable `FreeMatchBoard` / MySQL / approved TypeScript worker rehearsal, based on merged application PR #124 (`a14a529`). They contain no live player records or credentials. Rook and Nyx are fictional presentation labels, not the enrolled test identities.

`viewer.json` pins the complete package and free deployment profile for each match. The played example includes source-reported training credit in its disposable journal. The walkover includes no simulated replay or credit. The local chain clock was advanced to exercise deadlines and may be ahead of wall-clock time. These receipts describe that test chain, not a public deployment.

The fixtures deliberately retain the public wire data unchanged: raw receipt/event/state evidence, manifest, approved profile, opening and optional completion snapshot. Private strategies, encrypted worker inputs, database rows and signing keys are not included. The application has an optional paired test/export command documented in its `docs/FREE_SAMPLE_CONFORMANCE.md`.

The independent reader validates these exact hashes and internal relationships. A copied fixture or a freshly calculated pin is not proof that a production match occurred. See [the verification boundary](../../docs/free-matches.md).
